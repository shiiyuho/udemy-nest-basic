/* eslint-disable prettier/prettier */
describe('Jestの練習', () => {
  it('test1', () => {
    const result = 1;
    const exprected = 2;
    expect(result).toEqual(exprected);
    expect(result).toEqual(exprected);
  });
});
